Here, We have Header, footer and links section seperated from the main body i.e from the body tag of the index.html page because 
these two functions will be required in almost every pages that we create.
These functions will be there in almost every pages hence we seperated them so that we could call them with PHP where ever required.


Contents:
The UI design is made mostly from Bootstrap 5 






 
